/**
 * Define os valores padrão para as opções de organização e o intervalo de auto-organização.
 */

export const DEFAULT_OPTIONS = {
    sortBookmarks: true,     // Ordenar favoritos por nome
    mergeFolders: false,     // Unificar pastas com o mesmo nome
    removeDuplicates: false  // Remover URLs duplicadas
};

export const DEFAULT_INTERVAL_MINUTES = 5; // 5 minutos como padrão para testes
